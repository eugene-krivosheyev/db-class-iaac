# TODO: 
# temorary/session developer home folder
# internet access rules
# refactor to roles

- hosts: all
  gather_facts: no
  become: yes

  tasks:
  - name: "Prepare apt cache"
    ignore_errors: yes
    tags: apt
    apt:
      autoremove: yes
      autoclean: yes
      update_cache: yes
      cache_valid_time: 86400 #One day

- hosts: dev_stations
  gather_facts: yes
  become: yes

  pre_tasks:
    - assert:
        that:
          - ansible_processor_vcpus >= 4
          - (ansible_memtotal_mb * 0.001024) | int >= 12
          - ((ansible_mounts|first).size_available | float / 1000000000) >= 200
      tags: not_for_virtual_box_test_env

  tasks:
    - name: "Set static host ip: Update config"
      tags: network
      template:
        src: templates/01-network-manager-all.yaml.j2
        dest: /etc/netplan/01-network-manager-all.yaml
        owner: root
        group: root
        mode: u=rw,g=r,o=r

    - name: "Set static host ip: Restart service"
      tags: network
      command: netplan apply


    - name: Create developers group
      tags: user4developer
      group:
        name: developers
        state: present

    - name: Create developer user
      tags: user4developer
      command: adduser --disabled-password --gecos "" developer

    - name: Add administrator user to developers group
      tags: user4developer
      user:
        name: administrator
        groups: developers
        append: yes

    - name: Add developer user to developers group
      tags: user4developer
      user:
        name: developer
        groups: developers
        append: yes

    - name: Reset developer user password
      tags: user4developer
      command: passwd -d developer


    - name: Get rid of bluetooth
      tags: ux
      command: systemctl disable bluetooth.service
    
    - name: "Manage power settings: Copy power management config"
      tags: ux
      copy:
        src: logind.conf
        dest: /etc/systemd/
        owner: root
        group: root
        mode: u=rw,g=r,o=r

    - name: "Manage power settings: Apply config"
      tags: ux
      command: systemctl restart systemd-logind.service

    - name: "Manage hibernating settings: Copy config"
      tags: ux
      copy:
        src: grub
        dest: /etc/default/
        owner: root
        group: root
        mode: u=rw,g=r,o=r

    - name: "Manage hibernating settings: Apply config"
      tags: ux
      command: update-grub


    - name: "Install Ansible: Add apt repository"
      tags: ansible
      apt_repository:
        repo: ppa:ansible/ansible
        state: present

    - name: "Install Ansible: Update apt repository"
      tags: ansible
      apt:
        upgrade: yes
        update_cache: yes
        cache_valid_time: 86400 #One day

    - name: "Install Ansible: Install package"
      tags: ansible
      apt:
        name: ansible
        state: present


    - name: Install Git
      tags: git
      package:
        name: git
        state: present

    - name: Install JDK
      tags: java
      package:
        name: openjdk-8-jdk-headless
        state: present


    - name: "Install Maven: Install package"
      tags: java, maven
      package:
        name: maven
        state: present

    - name: "Install Maven: Make local repo folder"
      tags: java, maven, maven-repo
      file:
        path: /usr/share/maven-repo/
        state: directory
        owner: developer
        group: developers
        mode: u=rwx,g=rwx,o=r
        recurse: yes

    - name: "Install Maven: Configure local repo folder"
      tags: java, maven, maven-repo
      copy:
        src: settings.xml
        dest: /usr/share/maven/conf/


    - name: Install Gradle
      tags: java
      package:
        name: gradle
        state: present


    - name: "Install IntelliJ IDEA CE: IDE"
      tags: java, idea
      snap:
        name: intellij-idea-community
        classic: yes
        state: present

    - name: "Install IntelliJ IDEA CE: Settings"
      tags: java, idea
      copy:
        src: settings.zip
        dest: /home/developer/.IdeaIC2019.3/config/


    - name: "Prepare temporary directories: developer/Temp"
      tags: ux, java, maven-warm-up
      become_user: developer
      file:
        path: /home/developer/Temp
        state: directory


    - name: "Install Chromium: Remove Firefox"
      tags: chromium
      package:
        name: firefox
        state: absent

    - name: "Install Chromium: Install snap"
      tags: chromium
      snap:
        name: chromium
        classic: yes
        state: present        


    - name: "Install Docker: Docker CE"
      tags: docker
      package:
        name: docker.io
        state: present

    - name: Add developer user to Docker group
      tags: user4developer, docker
      user:
        name: developer
        groups: docker
        append: yes

    - name: "Install Docker: Get rid of Permission Denied"
      tags: docker
      command: chmod 666 /var/run/docker.sock

    - name: "Install Docker: Warm up Docker cache with useful images"
      tags: docker, docker-warm-up
      become_user: administrator
      command: docker pull openjdk:8-jre-alpine


    - name: "Warming up maven cache: Clone java project"
      tags: java, maven, maven-warm-up
      become_user: developer
      command: git clone --branch master --depth 1 https://github.com/eugene-krivosheyev/agile-practices-application temp_warmup_project
      args:
        chdir: /home/developer/Temp/

    - name: "Warming up maven cache: Resetting owning for Maven repo"
      tags: maven, maven-warm-up
      file:
        path: /usr/share/maven-repo/
        owner: developer
        group: developers
        mode: u=rwx,g=rwx,o=r
        recurse: yes

    - name: "Warming up maven cache: Build maven project"
      tags: java, maven, maven-warm-up
      become: yes
      become_user: developer
      become_method: sudo
      command: mvn clean verify
      args:
        chdir: /home/developer/Temp/temp_warmup_project

    - name: "Warming up maven cache: Remove build directory"
      tags: java, maven, maven-warm-up
      become_user: developer
      file:
        path: /home/developer/Temp
        state: absent


    - name: Remove apt dependencies that are no longer required
      tags: apt
      apt:
        autoremove: yes


    - name: Reboot
      tags: reboot
      command: shutdown -r now "Reboot triggered by Ansible script"
      async: 1
      ignore_errors: yes

    - name: Wait for instance up after reboot
      tags: reboot
      wait_for_connection:
        delay: 5
        timeout: 30
      ignore_errors: yes
